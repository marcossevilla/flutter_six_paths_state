name: flutter_six_paths_state
description: A Flutter project to compare the most used ways to manage state.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  bloc: ^7.0.0
  cupertino_icons: ^1.0.3
  equatable: ^2.0.3
  flutter_bloc: ^7.1.0
  flutter_mobx: ^2.0.1
  flutter_redux: ^0.8.2
  get: ^4.3.4
  meta: ^1.3.0
  mobx: ^2.0.3
  provider: ^5.0.0
  redux: ^5.0.0
  uuid: ^3.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.0
  mobx_codegen: ^2.0.2

flutter:
  uses-material-design: true
